> 문서: 02-workflow.md · 상태: 초안 완료 (남은 [?] 5개는 사소한 UX 디테일로 의도적으로 남겨둠) · 열린 질문 [?] 5개 · 채택한 [제안] 10개 · 마지막 갱신: 수요일
> 이 문서를 읽는 에이전트에게: 여기 없는 것은 지어내지 말고 질문으로 돌려라. [?] 는 팀이 아직 모르는 것이다.

※ 01-problem.md 가 아직 없어서, 01을 가리켜야 하는 자리는 모두 [?] 로 둔다. 01이 만들어지면 연결한다.
※ [채택] 구조(질문21 팀 답변 "맞아."): 원래 S2(Original 업로드)·S3(Reference 업로드)로 나뉘어 있던 단계를 "하나의 업로드 페이지(Original·Reference 두 슬롯, 순서 무관)"로 통합했다(질문20 팀 답변: "레퍼런스랑 오리지널 같은 페이지에 올릴 거야. 순서는 없어."). 이에 따라 S4 이후 단계 번호를 한 칸씩 당겨 재정렬했다. 아래 단계 표의 S 번호는 이 문서의 최종 번호다.

## 행위자
| A | 이름 | 역할 | 한 줄 상황 | 지금 쓰는 도구 |
|---|---|---|---|---|
| A1 | 일반 사용자 | 일반 사용자 (업로드~다운로드 전 과정을 혼자 수행) | 스노우나 틱톡 같은 앱으로 필터 입히듯이 쓸 것 같다 | 스노우, 틱톡 같은 필터 앱 (지금 실제로 색감 바꿀 때 쓰는 도구이자, 이 서비스에 대한 기대 경험이기도 함) |
| A2 | 시스템 | 자동 분석·자동 보정 파라미터 생성을 수행하는 주체(사람이 아님) | Original·Reference를 받아 색감 차이를 분석하고 자동으로 파라미터를 만든다 | 해당 없음 |

※ 팀 답변: "일반인용 하나. 일반인의 경우, 스노우나 틱톡같은 앱으로 필터 입히듯이 쓸 것 같다." — 행위자는 한 종류로 확정.
※ [채택] A2(시스템)는 03-requirements.md 질문6에서 팀이 "시스템을 별도로 등록하자"고 답해 추가함 — R5("시스템은 Original과 Reference의 색감 차이를 분석해서 자동으로 보정 파라미터를 생성할 수 있어야 한다")처럼 시스템이 주체인 요구사항을 표현하기 위한 행위자.
※ [채택] "지금 쓰는 도구" 해석(질문14 팀 답변): "그냥 색감만 바꾸는 어플이지. 내가 만드는 서비스는 레퍼런스 색감을 카피하는 거라 다르긴 한데, 경험으로 봐도 돼." — 스노우/틱톡은 (a) 지금 실제로 색감을 바꿀 때 쓰는 도구(단순 필터 적용)이면서 (b) 이 서비스의 기대 경험(비유)으로도 참고할 만함. 단, 우리 서비스는 "정해진 필터"가 아니라 "레퍼런스 사진의 색감을 분석해서 카피"한다는 점에서 기능적으로는 더 정교함(다름).
※ [채택 → 수정 채택] 로그인/계정: 필요 — 처음엔 "계정 필요없음"(발산질문A-1)이었으나, 프리셋 저장 기능과 상충되어 질문25에서 "계정 필요있음으로 바꾸자"로 번복. 이후 05-policy.md 질문4에서 다시 "로그인은 프리셋 저장(F9) 시점에만 필요, F2~F8은 전부 비로그인 가능"으로 정정됨. 로그인 UI는 S1~S9에 별도 단계로 추가되는 게 아니라, F9("프리셋 저장") 버튼을 눌렀을 때 뜨는 모달/화면으로 처리된다 — [해결됨].

## 단계 (As-Is → To-Be)
| S | 단계 | 행동(As-Is) | 감정 | 불편 | To-Be 한 줄 |
|---|---|---|---|---|---|
| S1 | 접속 | 해당 없음 (신규 경험) | 기대감 / 약간의 낯설음 | [?] | 랜딩페이지를 꾸며서 "사진 보정 서비스구나"를 느끼게 한다 |
| S2 | Original·Reference 업로드 (한 페이지, 두 슬롯, 순서 무관) | 해당 없음 (신규 경험) | 감각적이다, 나도 전문가처럼 보정할 수 있겠구나 (기대) | 업로드 대기 (시간이 걸릴 수 있음) | 업로드 진행 상태(로딩 표시)를 보여주고, 두 슬롯이 다 채워지면 사용자가 직접 "분석 시작"을 눌러 다음으로 넘어가게 한다 (04-features.md F2/R2·R3에서 이미 확정된 내용을 그대로 옮김 — 새로 묻지 않음) |
| S3 | 두 이미지 분석 | 해당 없음 (신규 경험) | 설렘, 진행률이 안 보이면 불안함 | 오래 걸려서 답답하다 (분석~생성 대기 시간, S3~S4 공통) | 진행 상태를 보여줘서 기다림을 덜 답답하게 한다 |
| S4 | 자동 Preset 생성 | 해당 없음 (신규 경험) | 자동으로 다 해준다는 신기함/안도감 | 오래 걸려서 답답하다 (분석~생성 대기 시간, S3~S4 공통) | 진행 상태를 보여줘서 기다림을 덜 답답하게 한다 (S3와 동일, 채택) |
| S5 | Original에 자동 적용된 결과 Preview | 해당 없음 (신규 경험) | 실망스러움 (기대와 다름) | 내가 원하는 색감이 아니다 | 자동 결과가 마음에 안 들어도 바로 직접 조절할 수 있게 한다 |
| S6 | 사용자가 파라미터 직접 수정 (실시간 Preview) | 해당 없음 (신규 경험) | [?] | 파라미터가 너무 많아서 뭘 만져야 할지 모르겠다 / 슬라이더 반영이 느리면 답답하다 / 되돌릴 방법이 헷갈린다 | 파라미터를 패널(Light/Color/HSL/Grading 등)로 그룹화 + WebGL 실시간 반영으로 지연 없이 보여줌 + Before/After·Reset 제공 |
| S7 | Before/After 비교, Strength 조절, Reset | 해당 없음 (신규 경험) | [?] | 되돌리기 헷갈림 (S6과 동일) | Before/After 비교와 Reset 기능으로 되돌리기 헷갈림을 해소한다 |
| S8 | 최종 확정 | 해당 없음 (신규 경험) | [?] | 확정하고 나면 더 못 고치는 건지 불안하다 / 확정 버튼이 뭘 의미하는지 헷갈린다 | 확정 버튼을 누르면 보정 완료된 사진이 크게 보이는 결과 페이지로 전환되고(→S9), 언제든 편집 화면으로 돌아가 계속 고칠 수 있음을 안내한다 |
| S9 | 사진(JPG/PNG) 다운로드 / XMP 다운로드 | 해당 없음 (신규 경험) | [?] (사소한 UX 감정 디테일이라 굳이 안 묻고 [?]로 둠 — 아래 정리 참고) | 사진만 받을지 XMP도 받을지 헷갈린다 | 사진 다운로드와 XMP 다운로드를 각각 독립된 버튼으로 명확히 구분해서 보여준다 (04-features.md F7/R13에서 이미 확정된 내용을 그대로 옮김 — 새로 묻지 않음) |

※ 단계 순서는 팀이 준 초안 흐름을 옮긴 뒤, 질문20~21로 업로드 단계를 통합해 재정렬한 것(접속 → Original·Reference 업로드[한 페이지] → 두 이미지 분석 → 자동 Preset 생성 → Preview → 파라미터 수정 → Before/After·Strength·Reset → 확정 → 다운로드). 팀 답변("신규 경험이지 따지고 보면.")에 따라 행동(As-Is) 칸은 전부 "해당 없음(신규 경험)"으로 고정하고, 감정/불편/To-Be 위주로 채운다.

※ S1 감정 [채택] 근거: A1의 "스노우나 틱톡같은 앱으로 필터 입히듯이 쓸 것 같다" 답변 — 필터 앱에 익숙한 사용자가 (필터 앱과 달리) "보정 서비스"라는 랜딩페이지 문구/디자인을 처음 볼 때 느낄 법한 반응을 추정. S1 불편은 팀이 언급하지 않아 [?]로 둠. To-Be는 팀 답변("랜딩페이지를 꾸며서 '사진 관련 보정 페이지구나'를 느끼게 할 거야")을 그대로 옮김.
※ [정정] 로그인 시점(05-policy.md P1): 처음엔 "S8(최종 확정)에 로그인 필요"였으나, 05-policy.md 질문4에서 "다운로드 자체는 비로그인에도 가능해. 로그인은 프리셋을 홈페이지에 저장할 때 쓰거나..."로 정정됨 — S2~S9(업로드~다운로드) 전체가 로그인 없이 가능하고, 로그인은 오직 "프리셋을 계정에 저장"할 때만 필요하다. 상세 규칙은 05-policy.md P1 참고.

※ [채택] 발산질문 B(얽힌 다른 사람): "얽힌 사람은 없음." — A1(일반 사용자) 혼자 완결되는 흐름으로 확정.

※ S2(통합 업로드) 감정은 원래 Original 업로드에 대한 팀 답변("일단 감각적이다를 느끼게 할 것이고, 나도 전문가처럼 보정할 수 있겠구나를 느낄 거야")을 그대로 옮긴 것 — Reference 업로드 쪽 감정은 팀이 별도로 언급하지 않았다. S2 불편 [채택] "업로드 대기"는 Original·Reference 양쪽에서 공통으로 나온 답변(질문4, 질문6)을 하나로 합친 것. S2 To-Be는 여전히 [?] (팀 언급 없음).

※ [채택] "오래 걸려서 답답하다" → S3~S4(분석~Preset 생성 대기) 불편, "내가 원하는 색감이 아니다" → S5(Preview 결과) 불편으로 확정. (질문5 팀 답변: "'오래 걸려서 답답하다'는 업로드 후 분석/Preset 생성을 기다리는 동안의 답답함", "'내가 원하는 색감이 아니다'는 자동 생성된 결과 Preview를 보고 나서 드는 불만") 팀이 두 단계를 하나로 묶어 답해 두 칸에 동일 문장을 적었다 — 더 세분화할지는 [?] 아님(팀이 이미 "구분 안 해도 됨" 취지로 답함, 질문5 참고).

※ S4 To-Be [채택]: S3의 To-Be("진행 상태를 보여줘서 기다림을 덜 답답하게 한다")를 S4에도 재사용 — 근거: S3·S4 불편이 "오래 걸려서 답답하다"로 동일하게 묶여 있어, 같은 해결책(진행 상태 표시)이 적용됨.

※ S5 감정 [채택] 근거: S5 불편으로 이미 기록된 "내가 원하는 색감이 아니다"에서 자연스럽게 따라올 감정을 추정. To-Be는 팀 답변("자동 결과가 마음에 안 들어도 바로 직접 조절할 수 있게 한다")을 그대로 옮김.

※ S6 불편 세 가지("파라미터가 너무 많아서 뭘 만져야 할지 모르겠다 / 슬라이더 반영이 느리면 답답하다 / 되돌릴 방법이 헷갈린다")는 팀이 질문 예시를 그대로 확인해 준 답변. 감정은 별도 언급이 없어 [?]로 둔다. S6 To-Be [채택] 세 가지:
  - "파라미터가 너무 많아서 모르겠다" → 파라미터를 Light/Color/HSL/Grading 등 패널로 그룹화한다 — 근거: 이 프로젝트가 "Lightroom 스타일 보정 파라미터"를 다룬다는 원래 과제 설명, Lightroom UI의 일반적 그룹화 패턴.
  - "슬라이더 반영이 느리면 답답하다" → WebGL(Three.js) 기반 실시간 렌더링으로 지연 없이 바로 반영한다 — 근거: 원래 과제 설명에 "실시간 미리보기는 브라우저 WebGL(Three.js)"라고 이미 명시됨.
  - "되돌릴 방법이 헷갈린다" → Before/After 비교와 Reset 버튼을 제공해 언제든 자동 생성 결과로 되돌릴 수 있게 한다 — 근거: 원래 사용자 흐름 초안에 "Before/After 비교, Strength 조절, Reset"이 이미 명시됨.

※ S7 불편 = "되돌리기 헷갈림" — 팀이 S6과 동일하다고 확인. S7 감정은 언급 없어 [?]로 둔다. S7 To-Be [채택] "Before/After 비교와 Reset 기능으로 되돌리기 헷갈림을 해소한다" — 근거: S6 To-Be(3)와 S7 자체가 가리키는 문제가 같음(S7이 곧 그 기능 단계).

※ S8 불편 두 가지("확정하고 나면 더 못 고치는 건지 불안하다 / 확정 버튼이 뭘 의미하는지 헷갈린다")는 팀이 질문 예시를 그대로 확인해 준 답변. 감정은 언급 없어 [?]로 둔다. S8 To-Be [채택] "확정 후에도 되돌릴 수 있는지를 버튼/문구로 명확히 안내한다" — 근거: 위 두 불편(불안함·버튼 의미 헷갈림)에 직접 대응.
※ [채택] 정책(질문15 팀 답변): "확정은 사진을 로컬로 받는 거고, 계속 고칠 수 있어." — 확정 후에도 편집 화면으로 돌아가 파라미터를 계속 수정할 수 있다. 즉 확정 후 "더 못 고치는 건지 불안하다"는 불편은 실제로는 걱정할 필요가 없는 상황 — 그 사실을 명확히 안내하는 것이 S8 To-Be의 방향.
※ [채택] 구조(질문16 팀 답변): "확정을 누르면 보정완료 사진이 크게 뜨는 페이지가 뜨고, 다운로드 버튼을 만들거야." — S8(확정)과 S9(다운로드)은 합치지 않고 별도 단계로 유지한다. S8 = 확정 버튼 → 보정 완료 사진이 크게 보이는 결과 페이지로 전환. S9 = 그 결과 페이지에서 사진/XMP 다운로드 버튼을 눌러 실제로 받는 단계.

## 흐름
S1 → S2 → S3 → S4 → S5 → S6 → S7 → S8 → S9 (메인 흐름 · S2는 Original·Reference 통합 업로드 페이지)

분기(예외 상황):
- S2 업로드 실패 시: [채택] 에러 메시지를 보여주고 그 자리에서 재시도를 요청한다 (질문17 팀 답변: "에러 메시지 보여주고 다시 시도 요청")
- 업로드 용량 제한: [채택] 파일당 최대 50MB (수정 답변: "용량 제한 50MB로 하자." — 이전 "용량 제한 없음"[질문18]을 대체)
- 업로드 가능 이미지 형식: [채택] JPG/PNG/HEIC/RAW 모두 지원 (질문22 팀 답변: "JPG/PNG, HEIC이나 RAW도 됨")
- 분석/생성(S3~S4) 실패 시: [채택] 에러 메시지를 보여주고 다시 시도하게 한다 (질문19 팀 답변: "에러 메시지와 함께 다시 시도할 거야." — S2 업로드 실패와 동일한 패턴)
- Original·Reference 업로드 순서: [채택] 순서 없음, 한 페이지의 두 슬롯에 순서 상관없이 채우면 된다 (질문20~21 팀 답변: "레퍼런스랑 오리지널 같은 페이지에 올릴 거야. 순서는 없어." / "맞아." — 이에 따라 S2/S3(옛 번호)를 하나의 단계로 통합)
- 여러 장(Original 또는 Reference를 복수) 업로드 가능한지: [채택] 불가 — Original·Reference 각각 한 장씩만 업로드 가능 (질문23 팀 답변: "한 장씩")
- 세션 이탈(새로고침/닫기) 시 업로드한 사진·조절값이 보존되는지: [채택] 보존되길 원함 — 새로고침/재접속해도 업로드한 사진과 조절해 둔 파라미터 값이 남아있어야 한다 (질문24 팀 답변: "데이터 보존이 되었으면 좋겠어.") ※ 구체적 구현 방식은 05-policy.md "상태값" 섹션에서 더 다룸: 질문2 팀 답변으로 "상태는 서버에 저장"이 확정되었고, 비로그인 구간(F2~F5)은 "[제안] 익명 세션 ID로 임시 저장 후 로그인 시 계정에 연결"이 아직 팀 확정 전이라 [?] 유지.
- Reference 사진을 바꾸고 싶을 때: [채택] 별도의 "Reference만 교체" 경로 없음 — S2(업로드 화면)로 돌아가 Original·Reference를 처음부터 다시 올린다 (발산질문A-3 팀 답변: "레퍼런스를 바꾸고 싶을 땐, 다시 레퍼런스와 Original 업로드 화면부터 다시 시작")
- 로그인/계정: [채택, 05-policy.md P1로 최종 정정] 로그인은 F9(프리셋 저장) 시점에만 필요, F2~F8(업로드~확정)은 전부 비로그인 가능. 로그인 UI는 F9 버튼 클릭 시 뜨는 모달/화면.
- 마음에 드는 프리셋 저장/재사용: [채택] 로그인 후 계정 기반으로 저장 가능 (04-features.md F9).
- 세션 이탈 시 데이터 보존의 구체적 구현 방식: [채택] 비로그인 구간(F2~F8)은 브라우저 로컬 저장소(localStorage 등)에만 저장, 서버 세션 없음. F9(프리셋 저장, 로그인 필요)부터는 서버에 계정과 함께 저장 (05-policy.md 질문7로 확정, 이전 "익명 세션" 제안은 거절됨). ※ Draft/Finalized "상태값" 자체를 서버가 아는지 여부는 05-policy.md에 [?]로 남아 있음(구현 설계 단계에서 재확인).

※ 참고(MVP 범위 밖): 발산질문A-4(모바일 환경 불편)에 대해 팀이 "모바일 APK로 만들 생각도 하는 중"이라고 답함 — 이는 현재 워크플로우의 채택/거절 대상이 아니라 향후 로드맵에 가까운 내용. 현재 MVP 흐름(웹 기반, React/WebGL)에는 영향 없음으로 처리하고, 모바일 앱 관련 결정은 이 문서 범위 밖으로 둔다.

※ [마무리 정리] 아래 5개 [?]는 팀에게 묻지 않고 그대로 열어둔 채 문서를 마무리한다 — 근거: 기능/정책에 영향을 주지 않는 감정·카피 톤 디테일이라 실제 화면을 만들 때(디자인/카피라이팅 단계) 자연스럽게 정해질 항목으로 판단:
  - S1 불편 (접속 단계에서 딱히 불편이 없을 수도 있음)
  - S6 감정, S7 감정, S8 감정, S9 감정 (팀이 불편/To-Be는 답했지만 감정 자체는 답하지 않음 — 톤 디테일)
  세션 이탈 시 데이터 보존의 구체적 구현 방식은 질문7로 확정됨(브라우저 로컬 저장소, 서버 세션 없음) — 위 흐름 섹션 참고.

## 검토했으나 제외
| 후보 | 출처(누구 초안 · [제안]) | 제외 이유 |
|---|---|---|
| "어떤 사진을 Reference로 골라야 할지 모르겠다"는 선택 부담을 S2의 불편으로 추가 | 발산질문 A-2 [제안] | 팀이 "거절"로 답함(이유 별도 언급 없음) |
| 결과물을 SNS에 공유해서 보는 친구/팔로워를 얽힌 사람으로 추가 | 발산질문 B-2 [제안] | 팀이 "얽힌 사람은 없음"으로 답함 |
| Reference 사진의 원저작자를 얽힌 사람으로 추가 | 발산질문 B-3 [제안] | 팀이 "얽힌 사람은 없음"으로 답함 |
